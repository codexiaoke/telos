export const name = 'telos-companion'
export const inject: readonly string[] = []

/** Desktop behavior is owned by Electron; the DSH host half is intentionally inert. */
export function apply(): void {}
