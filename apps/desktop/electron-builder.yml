appId: ai.telos.desktop
productName: Telos
copyright: Copyright © 2026 Telos contributors
asar: true
npmRebuild: false
compression: normal
afterPack: build/after-pack.mjs

directories:
  output: ../../dist
  buildResources: build

files:
  - out/**/*
  - package.json
  - '!out/**/*.map'

artifactName: ${productName}-${version}-${os}-${arch}.${ext}

publish:
  provider: github
  owner: codexiaoke
  repo: telos
  releaseType: draft

mac:
  category: public.app-category.productivity
  # Free open-source distribution: the first launch is approved by the user in
  # macOS Privacy & Security instead of relying on a paid Developer ID.
  identity: null
  hardenedRuntime: false
  gatekeeperAssess: false
  target:
    - dmg
    - zip

dmg:
  sign: false

win:
  # The complete DSH runtime makes the unpacked app larger than standard NSIS
  # can safely embed. The web installer and its payload are published together
  # on GitHub Releases, so no paid download service is required.
  target:
    - nsis-web

nsisWeb:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Telos

linux:
  category: Utility
  executableName: telos
  target:
    - AppImage
    - deb
